# ADR-0003: AI cannot emit direct VMF mutations

Status: accepted

AI outputs typed intent and ranking. Deterministic code produces and validates patches. This prevents probabilistic text generation from bypassing geometry, reference, compiler, and runtime gates.
