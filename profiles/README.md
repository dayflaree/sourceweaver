# Game profiles

Profiles describe branch-specific discovery, semantics, compiler behavior, runtime launch policy, tolerances, and feature support. They do not contain compiler binaries.

A profile file is a template until paired with a qualified executable-fingerprint record. SourceWeaver serializes and hashes the fully resolved profile into every run manifest.
